name: Test Reusable

on:
  workflow_call:

concurrency:
  group: "shoebox"
  cancel-in-progress: true

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello from reusable workflow!"
